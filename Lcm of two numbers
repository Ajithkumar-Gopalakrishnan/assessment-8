a=int(input("Enter the first number:"))
b=int(input("Enter the second number:"))
if(a>b):
    max=a
else:
    max=b
while(True):
    if(max%a==0 and max%b==0):
        print("LCM is:",max)
        break
    max=max+1
